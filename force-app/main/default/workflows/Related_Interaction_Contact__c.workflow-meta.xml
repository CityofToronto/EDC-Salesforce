<?xml version="1.0" encoding="UTF-8"?>
<Workflow xmlns="http://soap.sforce.com/2006/04/metadata">
    <fieldUpdates>
        <fullName>Update_Unique_Record_Key</fullName>
        <description>This Field update is to allow creating unique Related Interaction Contact Record i.e. one particular interaction can not be associated with same contact twice.</description>
        <field>Unique_Record_Key__c</field>
        <formula>Contact__c + Interaction__c</formula>
        <name>Update Unique Record Key</name>
        <notifyAssignee>false</notifyAssignee>
        <operation>Formula</operation>
        <protected>false</protected>
    </fieldUpdates>
    <rules>
        <fullName>Update Unique Record Key</fullName>
        <actions>
            <name>Update_Unique_Record_Key</name>
            <type>FieldUpdate</type>
        </actions>
        <active>true</active>
        <description>This workflow is to allow creating unique Related Interaction Contact Record i.e. one particular interaction can not be associated with same contact twice.</description>
        <formula>True</formula>
        <triggerType>onCreateOrTriggeringUpdate</triggerType>
    </rules>
</Workflow>
