<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account_Name_Required</fullName>
    <active>true</active>
    <description>This validation rule enforce to specify Account while creating any Contact records</description>
    <errorConditionFormula>AND(ISBLANK(AccountId),NOT(IsPersonAccount))</errorConditionFormula>
    <errorDisplayField>AccountId</errorDisplayField>
    <errorMessage>Account Name is required for Contact</errorMessage>
</ValidationRule>
