<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Active__c</fullName>
    <description>This defines if an Account is Active or Inactive. If an Account is Inactive then it is not activated so the downstream activities cannot be created off the Account</description>
    <externalId>false</externalId>
    <inlineHelpText>This defines if an Account is Active or Inactive. If an Account is Inactive then it is not activated so the downstream activities cannot be created off the Account</inlineHelpText>
    <label>Active</label>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>true</trackHistory>
    <type>Picklist</type>
    <valueSet>
        <restricted>true</restricted>
        <valueSetDefinition>
            <sorted>false</sorted>
            <value>
                <fullName>Yes</fullName>
                <default>true</default>
                <label>Yes</label>
            </value>
            <value>
                <fullName>No</fullName>
                <default>false</default>
                <label>No</label>
            </value>
        </valueSetDefinition>
    </valueSet>
</CustomField>
