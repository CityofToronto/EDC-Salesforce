<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Applicant_Address__c</fullName>
    <externalId>false</externalId>
    <formula>(IF(NOT(ISBLANK(Contact__r.MailingStreet)),Contact__r.MailingStreet+Br(),&#39;&#39;))+
(IF(NOT(ISBLANK(Contact__r.MailingCity)),Contact__r.MailingCity+Br(),&#39;&#39;))+
(IF(NOT(ISBLANK(Contact__r.MailingState)),Contact__r.MailingState+Br(),&#39;&#39;))+
IF(NOT(ISBLANK(Contact__r.MailingPostalCode)),Contact__r.MailingPostalCode+Br(),&#39;&#39;)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Applicant Address</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
